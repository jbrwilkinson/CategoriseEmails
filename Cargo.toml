[package]
name = "email-analyser"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "email-analyser"
path = "src/main.rs"

[dependencies]
walkdir = "2"
chrono = { version = "0.4", features = ["std"] }
clap = { version = "4", features = ["derive"] }
encoding_rs = "0.8"
